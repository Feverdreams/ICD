This program only need you to specify one argument, which is the path to the GEMs file to run.
For example, you can run it in command line with something like this:

python ICD_Yunze_Hao.py --path   ./BMI500/ICD/2018_I10gem.txt